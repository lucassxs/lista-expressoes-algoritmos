# Escreva um programa que receba um número real e apresente o valor de sus raiz quadrada.

import math
n = float(input('digite um número para extrairmos a raiz quadrada: '))
r = math.sqrt(n)
print('o valor da raiz quadrada do número é: {}!'.format(r))